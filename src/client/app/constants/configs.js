(function () {
    "use strict";

    angular.module('blog-project').constant('Config', {
        apiVersion: 'v1',
        domainName: 'http://localhost:3000' // leave empty for localhost
    });
})();